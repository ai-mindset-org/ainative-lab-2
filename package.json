{
  "name": "ainative-lab-2",
  "version": "0.1.0",
  "private": true,
  "description": "AI-Native Sprint Vol2 (s3) hub — context engineering playground, artifact chain, harness toolkit.",
  "scripts": {
    "deploy": "bash _scripts/deploy.sh",
    "test": "bash _scripts/test.sh"
  },
  "engines": {
    "node": ">=18"
  }
}
